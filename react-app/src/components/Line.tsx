import React from 'react';
import styled from 'styled-components'

const StyledLine = styled.div`
font-size: 24px;
color: white;
`

const Line = () => {
    return <StyledLine/>
};

export default Line;